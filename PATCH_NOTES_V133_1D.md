# V133.1D — Hotfix métriques, board chart & mobile readouts

- Corrige le crash `Can't find variable: metrics` lors de l'import de certains replays.
- Le bloc `Cartes restées en main` est de nouveau rendu après les tableaux de stats.
- Le graphique `Présence sur board` passe en courbes plutôt qu'en barres fines.
- Le lore potentiel est affiché comme une courbe discrète en pointillés.
- Les résumés des graphes `Cartes en main` et `Présence sur board` sont raccourcis pour éviter les débordements mobile.
- Le ZIP livré exclut `node_modules` et `dist`.
